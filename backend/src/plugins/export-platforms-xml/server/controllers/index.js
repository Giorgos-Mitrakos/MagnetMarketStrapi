'use strict';

const categoryController = require('./category-controller');
const exportController = require('./export-xml-controller');

module.exports = {
  categoryController,
  exportController,
};
